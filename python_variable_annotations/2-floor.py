#!/usr/bin/env python3
"""Function floor, that takes float as argument
and returns the floor of the float."""
import math


def floor(n: float) -> int:
    """return the floor of the float n"""
    return math.floor(n)
